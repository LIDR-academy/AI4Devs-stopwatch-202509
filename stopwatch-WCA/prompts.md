# Chatbot: ChatGPT version: GPT-4o

# Role

You are an expert developer in HTML and JS.

# Context

I have a requirement to create a web page (HTML with JS logic in script.js) that contains a stopwatch with two buttons: Start and Clear.

# Objective

Using the attached image as a reference, I want to create a web page using JS that allows me to:

 1. Display a counter in the format 00:00:00:000, including the last three digits (000) exactly as shown in the image.
 2. A Start button that starts the counter in ascending order (00:00:00:000). 3. A Clear button that resets the stopwatch.